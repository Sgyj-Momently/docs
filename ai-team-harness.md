# AI 팀 운영 모델 (Harness Engineering + Pair Programming)

이 문서는 `SgyjMoments` 워크스페이스를 “파이프라인별 2인 페어(개발+리뷰)”로 나눠 병렬 개발하면서도,
전체 워크플로 품질(계약, 상태 전이, 실패/재시도, 산출물 저장)을 흔들리지 않게 유지하기 위한 운영 규칙을 정의한다.

## 목표

- 파이프라인별 개발 속도를 유지하면서도, 모듈 간 계약을 안정적으로 고정한다.
- 단위 하네스(모듈 단독 실행/테스트)와 통합 하네스(오케스트레이터 E2E)를 함께 유지한다.
- 변경을 작게 쪼개어 “리뷰 가능한 단위 → 검증 → 누적” 흐름으로 누수를 줄인다.

## 팀 구성 (고정 역할)

### 1) 파이프라인별 Pair 팀 (Harness Pair)

각 파이프라인(모듈)은 아래 2인을 항상 한 팀으로 고정한다.

- **Driver (개발 담당)**
  - 구현/리팩터링/테스트/실행을 수행한다.
  - 변경을 작은 단위로 유지하고, 로컬 하네스가 통과되는 상태만 남긴다.
- **Navigator (리뷰·교정 담당)**
  - Driver 변경을 즉시 리뷰하고, 문제를 “같은 사이클 안에서” 고친다.
  - 계약/테스트 누락/엣지 케이스/성능·타임아웃/멱등성·재시도 관점에서 품질을 방어한다.
  - 불필요한 복잡도를 삭제/단순화하는 방향을 지속 제안한다.

권장 페어 분할(현재 레포 기준):

- **Pair-PhotoInfo**: `photo_exif_llm_pipeline`
- **Pair-Grouping**: `photo_grouping_agent`
- **Pair-Orchestrator**: `spring_orchestrator`

### 2) 중앙 에이전트 2종

- **Workflow Judge (전체 흐름 판단)**
  - 7단계 워크플로의 “순서/상태/실패·재시도/멱등성” 기준을 확정한다.
  - 이번 사이클의 최소 E2E 범위(예: 1→2단계 자동화)를 정의하고 범위를 지킨다.
  - 모듈별 요구사항 충돌을 조정해 “결정된 계약”으로 수렴시킨다.

- **Contract Router (정보 조율·전달)**
  - 모듈 간 전달되는 데이터(스키마/필드/아티팩트 경로)의 표준을 유지한다.
  - 공개 API와 내부 설정의 경계를 강제한다(주소/timeout/모델명 등은 내부 설정).
  - 민감정보/제외 정책이 다음 단계로 새지 않게 end-to-end로 확인한다.

> 중앙 에이전트는 “코드 양산자”가 아니라 “계약/흐름/품질의 수호자” 역할이 핵심이다.

## 개발 루프 (페어 프로그래밍 규칙)

### 페어 루프 (필수)

- Driver는 **작은 변경**을 만든다.
- Navigator는 즉시 리뷰하고, 필요한 수정은 **같은 사이클에서** 반영한다.
- 변경은 아래 3가지를 만족할 때만 “완료”로 취급한다.
  - **하네스 통과**(단위 테스트/스모크)
  - **계약 불변** 또는 “계약 변경 절차” 준수
  - **artifact/로그/에러 처리**가 운영 가능한 형태

### 변경 단위 (권장)

- 한 PR은 “한 가지 의도(한 단계/한 계약)”만 담는다.
- 계약 변경(PR A)과 대규모 리팩터(PR B)는 분리한다.

## 하네스(검증) 정의

### 단위 하네스 (모듈 단독)

- **PhotoInfo**: 로컬 입력 폴더 → bundle 생성 → (선택) blog 생성 → 산출물 계약 검증
- **Grouping**: 예제 입력 → 그룹화 결과 → 스키마/필드/점수 모델 검증
- **Orchestrator**: 상태 전이, 포트/어댑터, 프로필별 실행 경로를 테스트로 증명

### 통합 하네스 (오케스트레이터 E2E)

오케스트레이터의 실행 프로필로 “외부 의존성 수준”을 단계화한다.

- **stub 기반**: 외부 없이 상태/흐름 검증
- **local-photo-info**: photo info 단계는 로컬 CLI 실행으로 실제 산출물 생성
- **실HTTP 그룹화**: stub 없이 FastAPI 그룹화 서버를 실제 호출해 결과 저장

통합 하네스의 목적은 “풀 7단계”가 아니라, **이번 사이클에서 합의된 최소 단계**를 안정적으로 통과시키는 것이다.

## PR/리뷰 체크리스트 (Navigator 기준)

- **계약**
  - 입력/출력 필드가 문서(`docs/orchestrator-design.md`, `docs/contracts.md`)와 일치하는가?
  - 새 필드 추가 시 하위호환을 깨지 않는가? (필수 필드 추가 금지, 기본값/nullable 고려)
- **오류/재시도**
  - 타임아웃/네트워크 오류/비정상 종료의 실패가 “명확한 에러”로 기록되는가?
  - 재시도 시 중복 실행에 안전한가(멱등성)?
- **산출물**
  - 대용량 JSON/문서가 DB 본문에 들어가지 않고 artifact로 남는가?
  - artifact 경로가 예측 가능하고, 다음 단계가 참조할 수 있는가?
- **보안/개인정보**
  - `exclude_from_public_outputs` 정책이 다음 단계 입력에서 제거/마스킹되는가?
- **테스트**
  - 변경과 직접 관련된 테스트가 추가/수정되었는가?

## 운영 리듬 (권장)

- **데일리 동기화(짧게)**: Workflow Judge + Contract Router + 각 Pair Navigator가 계약/범위만 확인
- **계약 변경은 “먼저 문서/스키마 → 다음 코드” 순서**로 진행한다.

## 링크

- 전역 규칙: `../Agent.md`
- 프로젝트 개요: `./project-overview.md`
- 오케스트레이터 설계: `./orchestrator-design.md`
- 계약/아티팩트 규약: `./contracts.md`

