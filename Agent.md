# Workspace Agent Rules

이 문서는 `SgyjMoments` 워크스페이스 전체에서 공통으로 따르는 개발 규칙을 정의한다.
개별 프로젝트의 `AGENT.md`가 있으면 이 문서를 상속하고, 프로젝트 특성에 맞는 더 강한 규칙을 추가한다.

## 1. 작업 원칙

- 모든 변경은 작고 검증 가능한 단위로 나눈다.
- 새로운 기능과 버그 수정은 가능하면 테스트를 먼저 추가하거나, 최소한 같은 턴 안에서 검증까지 끝낸다.
- 문서, 코드, 테스트, 실행 방법은 가능한 한 같은 시점에 함께 갱신한다.
- 주석과 문서는 한글 우선으로 작성하되, 식별자와 외부 계약 키는 영어를 유지한다.

## 2. Git Flow 원칙

- 기본 브랜치 전략은 `Git Flow`를 따른다.
- 장기 유지 브랜치는 `main`과 `develop`을 기준으로 본다.
- 새로운 기능 개발은 반드시 `feature/*` 브랜치에서 시작한다.
- 릴리스 준비는 `release/*` 브랜치에서 진행한다.
- 운영 긴급 수정은 `hotfix/*` 브랜치에서 진행한다.
- 실험성 작업이라도 `main`에 직접 커밋하지 않고 목적에 맞는 작업 브랜치에서 진행한다.

## 3. 브랜치 명명 규칙

- 기능 개발: `feature/<project>-<short-topic>`
- 버그 수정: `bugfix/<project>-<short-topic>`
- 릴리스 준비: `release/<version-or-topic>`
- 긴급 수정: `hotfix/<project>-<short-topic>`
- 문서 전용 작업: `docs/<project>-<short-topic>`

## 4. 커밋 원칙

- 커밋 메시지는 가능하면 Conventional Commits 형식을 따른다.
- 권장 접두사는 `feat`, `fix`, `docs`, `refactor`, `test`, `chore` 이다.
- 한 커밋에는 하나의 의도만 담고, 무관한 정리 작업은 분리한다.
- 생성물, 실험 파일, 로컬 의존성 흔적은 커밋 전에 반드시 제외 여부를 확인한다.

## 5. 머지 원칙

- `main`에는 직접 커밋하지 않는다.
- `main`과 `develop` 반영은 PR 또는 머지 단위로 수행한다.
- 다른 브랜치의 기준선을 맞출 때는 가능하면 `fetch` 후 `rebase` 또는 명시적인 `merge`로 의도를 드러낸다.
- 히스토리를 덮어쓰는 강한 정리 작업은 팀 합의 없이 수행하지 않는다.

## 6. 검증 원칙

- 커밋 전에는 최소한 해당 변경과 직접 관련된 테스트를 실행한다.
- 테스트를 돌릴 수 없는 경우에는 이유와 미검증 범위를 기록한다.
- README의 실행 예시와 실제 CLI/엔드포인트가 어긋나지 않도록 함께 확인한다.

## 7. 금지 사항

- 사용자 승인 없이 `main`에서 직접 작업을 계속 진행하는 것
- 사용자 승인 없이 파괴적 Git 명령을 사용하는 것
- 원격과 동기화되지 않은 상태를 확인 없이 정상 상태로 간주하는 것
- 테스트 자산인지 개인 샘플 데이터인지 구분하지 않고 그대로 커밋하는 것
